import React from 'react';
import { device } from './device';
import styled from 'styled-components';

export function Subscribe() {
    return (
        <SubscribeForm>
            hello from Subscribe
        </SubscribeForm>
    );
}

const SubscribeForm = styled.form `

`
